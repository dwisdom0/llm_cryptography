[project]
name = "llm-cryptography"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.3.0",
    "peft>=0.17.1",
    "transformers>=4.57.1",
]
